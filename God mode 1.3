local humanoid = game.Players.LocalPlayer.Character.Humanoid

humanoid.HealthChanged:Connect(function(health)
    humanoid.Health = math.min(math.huge)
end)
--or

local RunService = game:GetService("RunService")
local humanoid = game.Players.LocalPlayer.Character.Humanoid

RunService.Heartbeat:Connect(function()
    humanoid.Health = math.min(math.huge)
end)
local player = game.Players.LocalPlayer -- get the local player
local character = player.Character or player.CharacterAdded:Wait() -- get the character model
local humanoid = character:FindFirstChildOfClass("Humanoid") -- get the humanoid object
if humanoid then -- check if the humanoid exists
    humanoid.Health = math.huge -- set the health to infinity
    humanoid.MaxHealth = math.huge -- set the max health to infinity
    humanoid.HealthDisplayDistance = math.huge
end
local Players = game:GetService("Players") -- get the Players service
local player = Players.LocalPlayer -- get the local player
player.HealthDisplayDistance = math.huge -- set the HealthDisplayDistance to 50
